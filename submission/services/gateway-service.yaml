apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: default
  labels:
    app: gateway-service
    tier: frontend
spec:
  type: NodePort          
  selector:
    app: gateway-service
  ports:
    - name: http
      protocol: TCP
      port: 3003
      targetPort: 3003
      nodePort: 30003    
